## 版本信息

- 版本号：v12
- Git 标签：`v12`
- 默认端口：
  - 后端：8000（`http://127.0.0.1:8000/`）
  - 前端：5175（`http://127.0.0.1:5175/`）

## 这一版包含什么

这是从 AI-DCP V12 开始整理出来的一套“可用于长期 Git 维护的干净源码工作区”，重点是：

- 源码可直接开发、可复现运行
- 不把运行态目录、依赖目录、密钥文件上传到 Git
- 用 Git tag/branch 管理版本

## 如何运行（macOS）

### 方式 A：一键启动（推荐）

前提：你已经完成过一次依赖安装（见下文“首次安装依赖”）。

- 启动：双击 `tools/web/start.command`
- 停止：双击 `tools/web/stop.command`

启动成功后打开：

- 前端：`http://127.0.0.1:5175/`
- 后端：`http://127.0.0.1:8000/`

日志查看：

- `./.run/backend-8000.log`
- `./.run/frontend-5175.log`

### 方式 B：手动启动（更可控，排查问题更方便）

1) 启动后端：

```bash
cd backend
./venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

2) 启动前端：

```bash
cd frontend
npm run dev -- --host 127.0.0.1 --port 5175
```

## 首次安装依赖

### 后端依赖（Python）

```bash
cd backend
python3 -m venv venv
./venv/bin/pip install -r requirements.runtime.v1.txt
./venv/bin/python -m playwright install chromium
```

### 前端依赖（Node.js）

```bash
cd frontend
npm install
```

## 配置与数据（重要）

- 运行日志目录：`./.run/`（不会上传到 Git）
- 运行数据目录：通常位于 `data/`、`logs/` 等（不会上传到 Git）
- 密钥/Token：不要写入代码文件；建议通过应用界面配置或使用本机环境变量/本地配置文件（`.env` 等不会上传到 Git）

## 常见问题

### 1) urllib3 / LibreSSL 警告

如果看到：

`urllib3 v2 only supports OpenSSL 1.1.1+ ... LibreSSL 2.8.3`

多数情况下只是警告；若遇到 HTTPS 请求异常，建议改用 Homebrew 的 Python 并重建 venv。

